#include <stdio.h>
#include <string.h>

char* longestPalindrome(char* s) {
    const int len = strlen(s);
    for (int i = 0; i < len; i++) {

    }
}

int main(void) {
    char str[1024];
    scanf("%s", str);
    return 0;
}